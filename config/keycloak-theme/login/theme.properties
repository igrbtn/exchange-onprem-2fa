# Custom login theme. parent=keycloak (classic server-rendered FTL) so it renders inside the
# Outlook embedded WebView; keycloak.v2 (React) does not.
parent=keycloak
import=common/keycloak

styles=css/login.css css/custom.css
